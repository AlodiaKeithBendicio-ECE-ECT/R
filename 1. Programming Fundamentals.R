# R is case-sensitive
# press CTRL + Enter to run

# FUNCTIONS (R)
# a body of reusable code used to perform specific tasks in R

# ARGUMENT (R)
# information that a function in R needs in order to run

print("Engr. Alodia")
?print()

# putting question mark before the function returns a page in the Help Window

# VARIABLE (R)
# a representation of a value that can be stored for later use
# can also be called 'object'

engr_variable <- "Alodia"
print(engr_variable)

num_variable <- 14.3

# VECTOR (R)
# a group of data elements of the same type stored in a sequence in R

vec_1 <- c(10, 9.28, 4.1, 7.4, 13, 6, 9.9)
vec_1
  
# PIPE (R)
# a tool in R for expressing a sequence of multiple operations
# represented with "%>%"
# used to apply the output of one function into another function
















